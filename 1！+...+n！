#include<stdio.h>
int main(void) {
	int n = 0;
	int ret=1;
	int sum = 0;
	scanf_s("%d", &n);
	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= i; j++)
			ret = j * ret;
		sum = sum + m;
		ret = 1;
	}
	printf("%d", sum);
	return 0;
}
