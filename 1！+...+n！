#include<stdio.h>
int main(void) {
	int n = 0;
	int m=1;
	int sum = 0;
	scanf_s("%d", &n);
	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= i; j++)
			m = j * m;
		sum = sum + m;
		m = 1;
	}
	printf("%d", sum);
	return 0;
}
